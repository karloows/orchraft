# macOS Finder metadata
.DS_Store
# npm/node dependency cache
node_modules/
# local env var overrides (direnv)
.envrc
# local MCP server config, may contain secrets/tokens
.mcp.json
# per-machine Claude Code permission overrides
.claude/settings.local.json
# internal roadmap, not for the shipped plugin
ROADMAP.md
# a maintainer's own orchraft settings; the shipped template is
# .orchraft.example.jsonc. Projects that install orchraft normally commit
# theirs so the whole repo shares one answer.
.orchraft.json
.orchraft.jsonc
*/config.toml